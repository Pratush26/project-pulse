"use client"

export default function FlagProjectBtn({ projectId, projectTitle }) {
    const handleClick = async () => {

    }
    return (
        <button onClick={handleClick} className="btn trns rounded-full btn-out">Flag</button>
    )
}